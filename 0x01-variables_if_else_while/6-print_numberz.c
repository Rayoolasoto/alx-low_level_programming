#include <stdio.h>
/**
 *  * main - Entry point
 *   * Return: 0 when successful
 *    */

int main(void)
{
		int nums;

			for (nums = 48; nums <= 57; nums++)
					{
								putchar(nums);
									}
				putchar('\n');
					return (0);
}
