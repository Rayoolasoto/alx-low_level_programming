#include <stdio.h>
/**
 *  * main - Entry point
 *   * Return: o when success
 *    */

int main(void)
{
		char nums;

			for (nums = 48; nums <= 57; nums++)
					{
								putchar(nums);
									}
				putchar('\n');
					return (0);
}
